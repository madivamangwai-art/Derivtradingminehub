# Workspace notes

- Keep the deployment stack aligned with Vercel + Supabase.
- Avoid rewriting published git history when pushing changes to the shared GitHub repository.
- Prefer idempotent database migrations so the schema can be applied safely in Supabase.
